<%= li_for_item(item) do %>
    <div class="view">
        <%= form_for_item_status(item) %>
        <label><%= item.description %></label>
        <%= button_to "x", user_list_item_path(current_user, item.list, item), method: :delete, class: "destroy" %>
    </div>
<% end %>